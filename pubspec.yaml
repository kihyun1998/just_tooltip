name: just_tooltip
description: "A customizable Flutter tooltip with directional placement (top/bottom/left/right), fine-grained alignment (start/center/end), hover & tap triggers, programmatic controller, fade animation, and RTL support."
version: 0.1.0
homepage: https://github.com/kihyun1998/just_tooltip
repository: https://github.com/kihyun1998/just_tooltip
issue_tracker: https://github.com/kihyun1998/just_tooltip/issues
topics:
  - tooltip
  - overlay
  - popup
  - widget
  - ui

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
